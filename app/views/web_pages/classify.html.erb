<p id="notice"><%= notice %></p>

<h1>Classify Layout Pattern of Web Pages</h1>

<%= render 'form' %>

<hr>

<%= link_to image_tag("screen-shots/#{@web_page.capture_image_path}", :width => '980'), asset_path("screen-shots/#{@web_page.capture_image_path}"), {:data => {:lightbox => "lightbox-#{@web_page.id}"}} %>

