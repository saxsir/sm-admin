<%= form_for(@layout_pattern) do |f| %>
  <% if @layout_pattern.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@layout_pattern.errors.count, "error") %> prohibited this layout_pattern from being saved:</h2>

      <ul>
      <% @layout_pattern.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :note %><br>
    <%= f.text_area :note %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
